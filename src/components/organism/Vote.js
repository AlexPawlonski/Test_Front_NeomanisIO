import React from 'react';
import PropTypes from 'prop-types';

export const Vote = ({ data, ...props }) => {
    return(
      <p>Vote</p>
    );
};

Vote.propTypes = {
 
};
Vote.defaultProps = {
  
};
export default Vote;