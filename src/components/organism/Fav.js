import React from 'react';
import PropTypes from 'prop-types';

export const Fav = ({ data, ...props }) => {
  
  return(
    <p>Fav</p>
  );
};

Fav.propTypes = {
 
};
Fav.defaultProps = {
  
};
export default Fav;